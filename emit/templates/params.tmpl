// Code generated by schemar. DO NOT EDIT.

package {{.Package}}

import (
	"net/http"
	"net/url"{{if .NeedsStrconv}}
	"strconv"{{end}}{{if .NeedsTime}}
	"time"{{end}}
)
{{range .Structs}}
// {{.Name}} holds query and header parameters for the {{.OpGoName}} operation.
type {{.Name}} struct {
{{- range .Fields}}
	{{- if .DocComment}}
	// {{.DocComment}}
	{{- end}}
	{{.GoName}} {{if .IsPointer}}*{{end}}{{.GoType}} `{{.StructTag}}`
{{- end}}
}

// encode converts the query parameters in p to URL query values.
func (p *{{.Name}}) encode() url.Values {
	q := url.Values{}
{{range .QueryEncodeBlocks}}{{.}}
{{end}}	return q
}

// encodeHeaders converts the header parameters in p to HTTP request headers.
func (p *{{.Name}}) encodeHeaders() http.Header {
	h := http.Header{}
{{range .HeaderEncodeBlocks}}{{.}}
{{end}}	return h
}
{{end}}
